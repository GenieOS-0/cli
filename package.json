{
  "name": "@genie-os/cli",
  "version": "0.1.0",
  "description": "The `genie` CLI — log in, manage keys, tail audit logs, send transactional emails from your terminal.",
  "license": "MIT",
  "homepage": "https://docs.genieos.pro/cli",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/GenieOS-0/cli.git"
  },
  "bugs": {
    "url": "https://github.com/GenieOS-0/cli/issues"
  },
  "type": "module",
  "bin": {
    "genie": "dist/cli.js"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsup && chmod +x dist/cli.js",
    "dev": "tsx src/cli.ts",
    "test": "node --test --import tsx test/*.test.ts",
    "prepublishOnly": "npm run build"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@genie-os/sdk": "^0.1.1",
    "tsup": "^8.5.1",
    "tsx": "^4.20.6",
    "typescript": "^5.5.4"
  },
  "keywords": [
    "genieos",
    "cli",
    "email",
    "transactional",
    "webhooks"
  ],
  "publishConfig": {
    "access": "public",
    "provenance": true
  }
}
