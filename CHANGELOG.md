# @genie-os/cli

## 0.1.4

- `genie links get` / `update` / `analytics`.
- `genie qr create` / `render` (formats incl. print PDF; `--save-to-assets`).
- Bundles `@genie-os/sdk@^0.1.5`.

## 0.1.3

- `genie links list` and `genie links utm-suggestions` — inspect existing
  links and reuse prior UTM values before create.

## 0.1.2

- `genie links create` accepts `--utm-source` / `--utm-medium` /
  `--utm-campaign` / `--utm-content` / `--utm-term`, plus `--tags`,
  `--campaign-id`, and `--domain`.

## 0.1.1

- `templates create` and `templates compose` commands.
- Expanded surface coverage aligned with `@genie-os/sdk` 0.1.2.
- Docs use `GENIEOS_API_KEY` / `gos_*` key format.

## 0.1.0

Initial release.
